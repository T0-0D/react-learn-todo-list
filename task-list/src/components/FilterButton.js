import React from "react";

function FilterButton (props) {
    return (
        <div>
          <button type="button">
            <span>All Tasks</span>
          </button>
        </div>
    );
}

export default FilterButton;
